# Intentionally empty. Do NOT `from .runner import main` here — see
# rocky_robot_mic/__init__.py for the double-import rationale. The
# sidecar is launched via `python -m rocky_mlx_stt.runner`.

__all__: list[str] = []
